1.你只需要修改win系统下资源管理器里面的工作目录项目代码即可,不需要你去wsl虚拟机到对应项目路径进行改代码,用户会自己将项目复制到wsl虚拟机进行程序测试验收,这是为了避免你出现各种问题以及导致大量token浪费, 而且根据项目不同也没有wsl虚拟机。
2.如果你不懂我的需求，或者细节上不懂，你要复述一遍我的需求,分析背景、细节是什么,并想清楚系统的接口契约。
3.遇到复杂需求时,你可以拆成多步,持续反问我的需求细节,以追求完美呼应你的想法。
4.让AI把需求用列表写下来,不要一次性全部制作,避免太多AI容易遗漏某些细节,自己测试也麻烦,让AI将大量需求拆分开来(少量1~2就算了),量多了就一点一点完成需求,然后再提示让先用户进行测试验收,用户自己也好测试不容易遗漏需求。
5.AI要勇敢指出当前代码中(非自己刚才写的情况下):
	- 职责不清或耦合过高的地方
	- 可能隐藏bug或者边界问题的逻辑
	- 并且反问是否需要优化代码
6.AI如果需要重构,重构代码要以:
	- 严格预防回归BUG出现
	- 本来我想说不要大规模重构"这条纪律，但是如果偏离了一开始定好的技术栈，那么也可以大规模重构，总之不能偏离定好的技术栈。
	- 遇到困难时，"不能用【马上验证成功的笨办法】"赢过"【理论更对但还没试过的正确办法】"，一定要贴合技术栈初衷的路，用技术栈里面的技术和特性。
	- 提高可读性，生成的让别的AI看到代码修护更加精确。
	- 降低复杂度
	- 让函数/模块职责更清晰
7.每次修护给出修改后的摘要信息,具体修改了哪个文件，并列出对应具体文件名和该文件的绝对路径(我更新文件就可以只复制修改过的文件，而不是整个项目的复制)。
8.AI用简短说明解释每一处重要修改的【原因】如果你发现设计层面的问题,可以适度调整结构,但请说明取舍。
9.一个Bug修了2~3次还没修好就有问题了，教导用户如何查看当前报错日志,让用户提供具体对应环境的报错日志而非猜测，避免对于顽固BUG一路走到底的死胡同,浪费时间又浪费token。
10.除了只能我肉眼进行的测试的功能,AI应该自动扫描代码使用相关自动化测试技术先测试并找找可能存在的bug,最后再交付代码给用户。
11.优先新增代码,不随意修改旧逻辑;不允许修改无关模块
12.新字段必须有默认值,不破坏旧序列化结构,不知道拿哪一个当默认值就询问用户
13.无法保证不影响旧功能时,必须先停止并给出风险说明
14.修改旧逻辑时必须同时生成回归测试
15.
16.不允许图省事，优先复用项目里已经正常在用的模式。
17.如果你有拒绝的行为，拒绝后请不要擅自改动我的代码！
18.你要学会【附带发现】,也就是说在我需求之外,但过程中顺带发现的问题,例如:我点名修护A,但是你要会顺手发现存在类似问题需要你要回这个。修护的修护B和修护C,不是我点名要查的,但是你顺手发现了,
19.我说的一些需求一开始无意，但随着更新，之前无意的一些地方是硬编码，所以一开始就要尽可能做成动态，或者说预留动态，最好可以提醒我某些地方不应该是硬编码，应该是动态。
20.能因为"这次是为了贴合技术栈"就跳过分步验收和回归测试这两道保险。
21.你要会修一个bug时不仅问"这一处怎么修",还要问"这是哪一类问题、还有没有同类问题需要修护。
22.安全性方面不要依赖调用方主动去守规则，要以调用方时而守规则时而不守规则的方式去对待所有的安全加固策略。
23.遇到困难时，不能用"马上验证成功的笨办法"赢过"理论更对但还没试过的正确办法"——一定要贴合技术栈初衷的路，用技术栈里面本来就有的技术和特性去解决问题，不要为了图快/图保险绕开已经选定的技术。
24."不要大规模重构"这条本来是对的纪律（防止无意义地大改已经跑得好好的代码、引入回归bug），但如果当前代码已经偏离了一开始定好的技术栈（比如引入某个技术进技术栈却只在小部分场景用、关键部位反而绕开不用），那么为了让代码重新贴合技术栈，可以大规模重构——总之目标是不能让代码长期偏离已经定好的技术栈。
25.项目里的静默相关的点，逐个排查每一处"静默"，先判断它是"疏忽"还是"故意的安全/防滥用设计"，然后只给前者补提示。
27.当你修护bug的时候，你可以自己创建一个"已知坑症状速查表"，方便下次遇到问题你自己可以找参考是否大同小异的方式处理解决。
28.只改必要代码，禁止全局扫描,禁止“顺便”帮你重构无用代码,禁止重复读取已存在的上下文。
29.回复极简化，让AI别说废话(去掉客套话和原理大篇幅讲解),只给修改结果,且修改代码时只给行段对比(Diff),不全量打印长代码。
30.限制路径，屏蔽无用文件，明确告诉AI绝对不能读取 .git、node_modules、dist、构建日志、图片和锁文件等,超长文件必须用关键字搜索而不是全量载入。
31.及时止损，设置熔断机制，连续修改失败 2 次必须强制停止,防止 AI 进入死循环疯狂烧钱。
32.配合.gitignore:让 AI 工具自带的检索器自动排除被忽略的文件。

---

# 项目：quietkey

Windows 托盘常驻工具。一个全局热键切换后台视频的播放/暂停，**不抢焦点、不切窗口**，
用户可以一边打笔记一边控制视频。技术栈 **Rust + egui**，仅 Windows，不做跨平台抽象。

## 命令

```powershell
cargo check                    # 改代码时的主要反馈手段
cargo run                      # debug 构建，保留控制台看日志
cargo run --release            # release 构建，无控制台窗口
cargo clippy --all-targets
```

`src/main.rs:1` 的 `windows_subsystem = "windows"` 只在 release 生效，
所以 **release 构建看不到任何日志**，排查一律用 debug 构建。
提高日志等级：`$env:RUST_LOG="debug"; cargo run`。

目前没有单元测试：几乎所有逻辑都是打在真实系统状态上的 Win32/WinRT 调用，
验收走界面里的**诊断**面板，见下方「验证改动」。

## 架构

两个线程，刻意分开：

- **事件循环线程** — eframe/egui 窗口 + 托盘图标。托盘**必须**在这里创建，
  Windows 上托盘图标属于拥有消息泵的那个线程（`src/tray.rs`）。
- **`hotkey-worker` 线程**（`src/main.rs` 的 `spawn_worker`）— 阻塞在
  `GlobalHotKeyEvent::receiver()` 上，整条策略链在这里跑。

分开不是随手写的：策略链可能阻塞 100ms 以上（`SendMessageTimeoutW`），
而主窗口平时是隐藏的，根本没有稳定的重绘节奏可依赖。
**不要把动作逻辑挪回 UI 线程。**

跨线程的只有 `Arc<Shared>`（`src/state.rs`）：进去的是 config 快照，出来的是 `ActionReport`。
UI 改的是 `draft: Config`，只有点「保存并应用」才写回 `Shared`——所以调设置不会打断正在进行的热键动作。

## 策略链——项目的核心

`src/strategy/mod.rs` 按**对用户的打扰程度**从小到大跑四层，第一个 `Outcome::Applied` 即停：

1. `gsmtc.rs` — Windows 媒体会话（`GlobalSystemMediaTransportControlsSessionManager`）
2. `messages.rs::appcommand_play_pause` — `WM_APPCOMMAND` 定向投给指定 HWND
3. `messages.rs::post_space` — `WM_KEYDOWN`/`WM_KEYUP` 投给焦点子窗口
4. `fallback.rs` — 抢前台 + `SendInput` + 还原。**默认关闭**

以下几条是设计依据，忘掉就会被改错：

- **合成的 `WM_KEYDOWN` 对浏览器视频无效。** Chromium 的键盘输入走自己的 renderer
  管线，不读顶层 HWND 的消息队列。对网页视频（本项目的主场景）来说第 1 层不是优化，
  而是唯一可行的无感路径。**不要以「精简」为由删掉 GSMTC。**
- **第 2 层用 `SendMessageTimeoutW` 而非 `PostMessageW`，是刻意的。** 返回值是区分
  「对方接住了」和「对方无视了」的唯一依据——处理 `WM_APPCOMMAND` 的程序返回 TRUE，
  `DefWindowProc` 返回 0。换成 `PostMessageW` 会让第 2 层永远报成功，
  把第 3、4 层永久饿死。
- **第 3 层无法确认任何事。** `PostMessageW` 是异步的，只表示「塞进队列了」。
  它乐观返回 `Applied`，detail 里带「无法确认」字样。这也正是每一层都要能单独开关的原因。
- **只有第 4 层是 `intrusive: true`。** UI 会标黄并警告。新增层时保持这个标记准确。

每次尝试都记进 `ActionReport::trace` 并在 UI 显示。**这条 trace 是主要排查手段，改策略链时要保留。**

## 第 1 层挑哪个媒体会话

`gsmtc::toggle` 按五级顺序挑，命中的路线会写进 trace（`chrome → 已暂停（最近活跃）`）：

1. **配置匹配** —— `target.exe` 填了进程名就永远听配置的
2. **最近活跃** —— 见 `strategy/recency.rs`，`follow_recent_session` 控制
3. **唯一在播放** —— 只开了一个视频的日常场景
4. **系统当前会话** —— `GetCurrentSession`
5. **唯一会话**

两条容易改错的依据：

- **第 3 条不能单独存在。** 视频被我们暂停后，正在播放的会话数变成 0，
  只靠它会彻底找不回目标，掉到对浏览器无效的第 2/3 层——症状是
  「能暂停、再按一次没反应」。第 4、5 条就是为这个补的。
- **第 2 条的判据是 `TimelineProperties.LastUpdatedTime`，不是播放状态。**
  实测（Chromium 系）：手动播放/暂停/拖进度条会刷新它，**一直播着不会**——
  它不随播放进度自己往前跑。所以它就是"这个会话上次被碰是什么时候"，
  取最大的即可，`recency.rs` 因此是**无状态纯函数**。

  走过两条弯路，别再回去：

  * **订阅 `PlaybackInfoChanged` 事件**：订阅得上（拿得到 token）但**回调一次都不触发**。
    GSMTC 的事件由中等完整性级别的系统媒体服务回调进来，程序一旦以管理员身份运行，
    跨完整性级别的入站 COM 调用会被系统拦掉，功能静默失效；而出站调用
    （枚举会话、切播放状态）全都正常，极难察觉。
  * **比对播放状态快照**：盲区是「手动播放→再手动暂停」这种一来一回，
    净状态和上一轮一模一样，比对结果是"没动过"，热键跟不过去——
    而这恰恰是最常见的用法。这条弯路还附带要求"把自己造成的变化回填快照"，
    一整套记账逻辑，换成时间戳后全部消失。

## 目标窗口解析

`TargetSpec`（`src/config.rs`）存的是**匹配规则**（进程名/标题子串/类名），
**绝不存 `HWND`**——目标程序一重启句柄就失效。
`resolve_target`（`src/win/list.rs`）每次触发都重新解析，多个命中时取标题最长的
（浏览器真正的视频标签页标题比「新标签页」长）。

窗口拾取器**故意不填** `title_contains`：视频标题会随播放进度变，拿它匹配很脆。

`list_windows` 过滤了 owner 窗口、`WS_EX_TOOLWINDOW`、以及 **DWM cloaked** 窗口。
cloaked 检查是必需的，去掉之后拾取器里会塞满看不见的 UWP 幽灵窗口。

## 不显然的约束

- **界面主题**：所有配色集中在 `src/theme.rs`（Win7 Aero 风浅色），
  别再往各处 `RichText::color(...)` 里塞硬编码颜色。egui 0.35 给亮/暗各存一套
  `Style`，所以要用 `ctx.all_styles_mut` 两套都写，并 `set_theme(Light)` 钉死，
  否则用户系统切深色时配色会散掉。
  **标题栏和窗口边框由 Windows 绘制，改不了**——除非关掉系统装饰自绘，
  那就要自己实现拖拽、双击最大化、贴边、缩放，是刻意没做的取舍。
- **截图验证界面时先 `SetProcessDPIAware()`**。这台机器是 150% 缩放，
  不做 DPI 感知的截图进程拿到的 `GetWindowRect` 是虚拟化坐标，
  而 `CopyFromScreen` 用物理像素，结果是**截到窗口左上角一部分**，
  看起来像"界面文字被裁掉了"——排查过一轮假 bug，别再上当。
- **中文字体**：egui 自带字形只有拉丁字符。`install_cjk_font`（`src/app.rs`）必须加载
  系统字体，否则整个界面是豆腐块。优先纯 `.ttf`（`Deng.ttf`、`simhei.ttf`），
  `.ttc` 字体集合在字体后端里支持不稳。
- **COM 套间**：GSMTC 是 WinRT。worker 线程在任何媒体会话调用前先
  `CoIncrementMTAUsage`。漏掉的话第 1 层会在运行期报一个很难懂的 HRESULT。
- **托盘事件需要显式唤醒**：窗口隐藏时 egui 不会自己重绘，所以 `tray::wire_wakeup`
  装了调用 `request_repaint()` 的处理器。没有它，点托盘菜单**毫无反应**。
- **关窗不能退出**：`App::update` 拦截 `close_requested`，发 `CancelClose` + `Visible(false)`。
  真正退出只走托盘的「退出」项，它会先置 `quit_flag` 再发 `Close`。
- **`force_foreground` 必须做 `AttachThreadInput` 那套动作**（`src/win/focus.rs`）。
  后台进程裸调 `SetForegroundWindow` 会被 Windows 的前台锁定机制拒绝。
- 热键注册在组合键被别的程序占用时会**直接失败**，这是 `RegisterHotKey` 的固有行为，
  不是 bug。要把错误暴露给用户，**不要静默吞掉**。

## 验证改动

`cargo check` 能挡住大部分问题，但 Win32/WinRT 层只能实机验证：

1. `cargo run`（debug，才有日志）
2. 起一个视频——浏览器验第 1、2 层，VLC 这类原生播放器验第 2、3 层
3. 诊断 →**刷新媒体会话**，确认 GSMTC 能看到播放器
4. 诊断 →**立即测试一次**，用界面上的草稿配置跑一遍（不必先保存），
   然后看「上次触发」里的逐层 trace
5. 把焦点放在**第三个**窗口（文本编辑器）里按真实热键，确认打字不被打断——
   这才是本项目真正的验收标准

要单测某一层，就把它上面的层在**策略链**区域关掉。

## 依赖注意

`windows`、`global-hotkey`、`tray-icon` 三个 crate 在小版本间会改 API
（HWND newtype 形态、参数变 `Option<HWND>`、事件枚举变体）。
升级挂了就改调用处，**不要为了图省事回退到老版本**。
`windows` 需要的 feature 列在 `Cargo.toml`，新增 Win32 调用通常也要在那里补 feature。
